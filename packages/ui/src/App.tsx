export default function App() {
  return (
    <main className="app-shell">
      <div className="app-frame">
        <p className="eyebrow">Agent Mesh</p>
        <h1>Local daemon UI scaffolding is ready.</h1>
        <p className="lede">
          The browser app now builds as a workspace package and ships with the CLI.
          The next task wires the real panels for overview, agents, sessions, transcripts, tasks, exposure, and logs.
        </p>
      </div>
    </main>
  );
}
